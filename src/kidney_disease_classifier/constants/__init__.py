
from pathlib import Path  # Import the Path class from the pathlib module for handling file paths

# Define the path to the configuration file, which is expected to be located in the 'config' directory
CONFIG_FILE_PATH = Path("config/config.yaml")

# Define the path to the parameters file, which is expected to be in the same directory as the script
PARAMS_FILE_PATH = Path("params.yaml")